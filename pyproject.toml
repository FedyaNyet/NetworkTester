[project]
name = "networktester"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "plotly>=6.3.0",
    "aiohttp>=3.9.0",
]

[project.scripts]
netmon = "netmon.monitor:main"
analyze = "netmon.analyze:main"
